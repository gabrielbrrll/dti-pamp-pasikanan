import { styled } from 'stitches.config'

const Input = styled('input', {
  background: '$snow',
  border: 'none',
  outline: 'none',
  height: '38px',
  padding: '8px 18px',
  color: '$charleston',
  borderRadius: '10px',
  fontSize: '16px',
  width: '100%',
})

export default Input
